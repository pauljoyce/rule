<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.clinical.dao.master.StandardResultMapper">

    <select id="selectStandardResultListById" resultType="java.lang.String" parameterType="java.lang.String">
        select
               standard_result
        from
             nlp_standard_table
        where standard_table_id = #{id}
    </select>

    <select id="selectStandardResultById" resultType="java.lang.String" parameterType="java.lang.String">
        select
            standard_result
        from
            nlp_standard_table
        where standard_table_id = #{id}
    </select>

</mapper>